-- Note for sqlite3 command
--


-- Schema for the table base 

declare @account_table_name as varchar(128) = 'accounts_table'

create table [IF NOT EXISTS] @account_table_name (
    accound_no int not null auto_increment ,


)